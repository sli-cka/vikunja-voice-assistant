language: "en"
intents:
  VikunjaAddTask:
    data:
      - sentences:
          - "add (a) task {task_description}"
          - "create (a) task {task_description}"
          - "great (a) task {task_description}"
          
lists:
  task_description:
    wildcard: true