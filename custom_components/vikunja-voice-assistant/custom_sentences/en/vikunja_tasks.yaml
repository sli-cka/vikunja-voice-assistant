language: "en"
intents:
  VikunjaAddTask:
    data:
      - sentences:
          - "add (a) task {task_description}"
          - "create (a) task {task_description}"
          - "add (a) {task_description} task"
          - "create (a) {task_description} task"
lists:
  task_description:
    wildcard: true